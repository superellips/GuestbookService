apiVersion: apps/v1
kind: Deployment
metadata:
  name: "guestbookservice-deployment"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "guestbookservice-app"
  template:
    metadata:
      labels:
        app: "guestbookservice-app"
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - name: guestbookservice-container
        image: ghcr.io/superellips/guestbookservice:41bba23d36c89e26d56be41d37f262a408e6aae6
        # resources:
        #   limits:
        #     cpu: "10"
        #     memory: "100Mi"
        #   requests:
        #     cpu: "10 "
        #     memory: "50Mi"
        ports:
        - containerPort: 8080